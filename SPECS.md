# Profile Site Specs

## 概要

Akira Kuriyama のプロフィールサイト。GitHub Pages でホスティング。
1ページ構成、JavaScriptなし、HTML + CSS のみ。

## コンテンツ

- **アイコン**: https://avatars.githubusercontent.com/u/964694
- **favicon**: GitHubアバターと同じ画像を使用
- **名前**: Akira Kuriyama
- **肩書き**: Software Engineer
- **リンク元**: `links.md` に記載されたリンク集をもとに構成

### セクション構成

1. **Blogs & SNS** - X, Zenn, Hatena Blog, Qiita, Medium, Speaker Deck
2. **会社のテックブログに書いた記事一覧** - Gist リンク1件
3. **登壇資料** - Speaker Deck のスライド一覧（サムネイル付き）

## デザインテイスト

- **テーマ**: 雪の白樺の森
- シャープで落ち着いていて、清々しい、静謐な冬の朝
- 大人っぽく品がある。子供っぽい装飾は避ける
- シンプルで見やすい

## デザイン詳細

### カラーパレット

- ページ背景: 白寄りの雪原グラデーション（上 `#f3f4f6` → 中 `#f8f8fa` → 下 `#fbfbfc`）
- ヘッダー背景: 白樺の幹のグレー、グラデーション（`#7b7b7a` → `#555049`）。明→暗。ワンポイント
- ヘッダーテキスト: 雪のオフホワイト (`#f2f0ed`)、サブテキスト (`#b0ada8`)
- テキスト: 冬の曇天のダークブルーグレー (`#2d3038`)
- セクションタイトル: ミドルグレー (`#7a7d85`)
- ボーダー・補助色: 霜のグレー (`#e0e2e6`)
- サブテキスト: (`#9a9da5`)
- シャドウ: 控えめな黒影 (`rgba(0, 0, 0, 0.05)`)

### レイアウト

- 最大幅: 880px、中央寄せ
- **リンクカード**: `repeat(auto-fill, minmax(250px, 1fr))` のグリッド
- **登壇資料カード**: 3列固定 (`repeat(3, 1fr)`)、コンパクトなサイズ
- レスポンシブ: 540px以下で登壇資料2列、400px以下で1列

### カード

- すりガラス風: `rgba(255,255,255,0.8)` + `backdrop-filter: blur(4px)`
- border-radius: `6px`（シャープ寄り）
- 下ボーダー: 常時 `2px solid #d0d2d6`、ホバー時にヘッダー色 `#7b7b7a` に変化
- ホバー: 控えめな `translateY(-2px)` と薄い影のみ
- 登壇資料のサムネイルはホバーで `scale(1.03)` の控えめなズーム

### セクションタイトル

- `uppercase` + `letter-spacing: 0.1em`
- 下線ボーダーで区切る

### ヘッダー

- 白樺の幹のようなグレーのダーク背景で、ページ全体のワンポイントとして締める
- 微かなグラデーション（`135deg`, `#7b7b7a` → `#555049`、明→暗）
- テキストは白系。装飾的なライン・カラーバーは不要

### ページ背景

- 上から下への縦グラデーション（`#f3f4f6` → `#f8f8fa` → `#fbfbfc`）
- 白に近い雪原のイメージ

### フッター

- コピーライトのみ。気の利いた一言系のコピーは入れない
- 上部に `1px solid #e0e2e6` の区切り線

## NGリスト

- ドットパターンなどの背景テクスチャ
- アイコンの回転・拡大などの派手なホバーエフェクト
- カードの `rotate` アニメーション
- カラフルなグラデーションライン
- フッターのキャッチコピー（「Good coffee, good code.」等）
